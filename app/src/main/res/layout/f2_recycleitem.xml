<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_margin="3dp"
    card_view:cardBackgroundColor="@android:color/darker_gray"
    card_view:cardCornerRadius="10dp">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:padding="10dp">
        <TextView
            android:id="@+id/desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="看别人的经历"
            android:textColor="@color/flush_title_color"
            android:textSize="15sp"/>

        <ImageView
            android:id="@+id/girl_pic"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_centerInParent="true"
            android:scaleType="centerCrop" />
        <TextView
            android:id="@+id/name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="看别人的经历"
            android:textColor="@color/flush_title_color"
            android:layout_alignBottom="@+id/girl_pic"
            android:layout_alignRight="@+id/girl_pic"
            android:layout_alignEnd="@+id/girl_pic"
            android:textSize="20sp"/>
    </RelativeLayout>

</android.support.v7.widget.CardView>
